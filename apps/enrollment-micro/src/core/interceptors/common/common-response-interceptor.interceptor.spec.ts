import { CommonResponseInterceptorInterceptor } from './common-response-interceptor.interceptor';

describe('CommonResponseInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new CommonResponseInterceptorInterceptor()).toBeDefined();
  });
});
